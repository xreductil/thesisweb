{
  "builds": [
    { "src": "server.py", "use": "@vercel/python" }
  ],
  "routes": [
    {
      "src": "^/api/(.*)$",
      "dest": "server.py"
    },
    {
      "src": "^/(.*)$",
      "dest": "server.py"
    }
  ]
}
